/**
 * Shared constants for cookie consent storage and event coordination.
 * Imported by both CookieConsent (banner) and AdSenseLoader (script gate).
 */

export const CONSENT_KEY = "cookie_consent";
export const CONSENT_EVENT = "cookie-consent-change";
